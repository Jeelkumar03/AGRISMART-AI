venv/
__pycache__/
*.pyc
.DS_Store

# dataset is too large for git -- share via drive link instead
data/train/
data/val/

# trained weights are large binaries -- regenerate with train.py or share separately
*.pth
